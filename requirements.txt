# Install PyTorch FIRST, separately -- the right command depends on your
# GPU/driver. See https://pytorch.org/get-started/locally/ then:
#   pip install -r requirements.txt

torch_geometric>=2.4
transformers>=4.30
librosa>=0.10
soundfile>=0.12
scikit-learn>=1.3
pandas>=2.0
tqdm>=4.65
matplotlib>=3.7
